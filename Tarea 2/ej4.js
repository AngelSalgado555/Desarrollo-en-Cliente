let arrayNumbers = [];
for (let index = 0; index <= 100; index++) {
        if (index % 2 == 0){
            var result = index;
            arrayNumbers.push(result);
            alert(arrayNumbers);
        } else {
            null;
        }
}